<div class="container" id="request_show">
  <%= render "shared/navbar-back-menu" %>
  <div>
    <h1><%= @request.user.name %></h1>
    <%= turbo_stream_from @request %>
    <div id="messages">
      <% @request.messages.each do |message| %>
        <%= render "messages/message", {message: message} %>
      <% end %>
    </div>
    <div class="form-check form-switch d-flex flex-row-reverse justify-content-center gap-1" data-controller="toggle">
      <label class="form-check-label" for="checkNativeSwitch" data-toggle-target="label">
        Hold Item
      </label>
      <input class="form-check-input" type="checkbox" value="" id="checkNativeSwitch" switch data-action="change->toggle#changeText">
      <div id="request_form">
        <%= simple_form_for(@request) do |f| %>
          <%= f.input :pickup_date, as: :date, html5: true, wrapper_html: { class: "bg-white border-primary" } %>
          <%= f.submit "Confirm", class: "btn btn-primary rounded-3 text-white d-flex flex-row me-2" %>
        <% end %>
      </div>
    </div>
    <div class="my-3 w-100">
      <%= simple_form_for [@request, @message], html: { class: "d-flex align-items-center w-100" } do |c| %>
        <%= c.text_field :content,
        class: "form-control me-2 rounded-3 border-primary bg-white flex-grow-2",
        placeholder: "Enter your message"
        %>
        <%= c.submit "Send", class: "btn btn-primary rounded-3 text-white" %>
      <% end %>
    </div>
  </div>
</div>
