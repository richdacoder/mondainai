<div class="container" >
  <div>
    <h1>Request
      <h1>
        <%= @request.user.name %>
        <% @request.messages.each  do |message|%>
          <%= message.content %>
          <%= message.created_at.strftime("%b %e, %l:%M %p") %>
        <% end %>
      </div>
    </div>
